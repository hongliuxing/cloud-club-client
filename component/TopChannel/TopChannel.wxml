<view class='comp-top-channel'>
    <block wx:for="{{btns}}" wx:for-index="idx" wx:for-item="b" wx:key="title">
        <btn title="{{b.title}}" btype="{{b.btype}}" bvalue="{{b.value}}"></btn>
    </block>
</view>
